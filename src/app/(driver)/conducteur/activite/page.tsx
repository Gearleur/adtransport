'use client'

/* ============================================================
   app/(driver)/conducteur/activite/page.tsx
   ============================================================ */

import { DriverActivity } from '@/features/driver/components/driver-activity'

export default function ActivitePage() {
  return <DriverActivity />
}